You are annotating Arabic masking data for the `{dataset_mode}` project.

Current chunk:
- chunk id: `{chunk_id}`
- file: `{chunk_file}`
- path: `{chunk_path}`
- rows: `{row_count}`

Instructions:
- {output_csv_instruction}
- Keep `original_text` unchanged.
- Fill only `masked_text`; all metadata will be generated automatically afterward.
- {row_id_instruction}
- Use typed placeholders like `[PERSON_1]`, `[LOC_1]`, `[ORG_1]`.
- Restart numbering per row.
- Keep numbering contiguous within each mask type in a row.
- Reuse the same placeholder for repeated mentions of the same entity in the same row.
- If uncertain, include `[UNCERTAIN]` or `[REVIEW]` somewhere in `masked_text`; the app will mark that row as pending automatically.
- Do not rename columns or add extra metadata columns.
- Return the full edited CSV only, covering every row in the chunk.
- Prompt row-id context enabled: `{include_row_id_in_prompt}`
- Row matching mode: `{row_matching}`
- Validate original_text against row_id: `{validate_original_text_with_row_id}`

Agent guidelines:
{guidelines}

Raw CSV for the whole chunk:
```csv
{chunk_csv}
```
